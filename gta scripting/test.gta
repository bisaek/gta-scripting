callout "callout 1" {
	when(getSuspectDistanceToPlayer()<30) {
		print(getSuspectDistanceToPlayer())
		startPursuit()
	}
	spawnSuspect()
}

callout "callout 2" {
	when(isYPressed()) {
		print(1 > 2)
	}
	spawnSuspect()
}