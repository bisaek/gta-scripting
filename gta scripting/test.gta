callout "callout 1" {
	when(isYPressed()) {
		print("dette er den første callout")
	}
	spawnSuspect()
}

callout "callout 2" {
	when(isYPressed()) {
		print("dette er den anden callout")
	}
	spawnSuspect()
}