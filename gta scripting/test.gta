callout "callout 1" {
	when(getSuspectDistanceToPlayer()<30) {
		print(getSuspectDistanceToPlayer())
	}
	spawnSuspect()
}

callout "callout 2" {
	when(isYPressed()) {
		print(1 > 2)
	}
	spawnSuspect()
}